default:
  suites:
    default:
      paths:
        - "%paths.base%/features"  # Pfad zu den Feature-Dateien
      contexts:
        - FeatureContext            # Kontext-Datei, in der die Schritte implementiert sind
  extensions:
    Behat\MinkExtension:
      base_url: "http://localhost/todo_list"  # Basis-URL Ihrer Anwendung
      default_session: "goutte"               # Standard-Sitzung
      sessions:
        goutte:
          goutte: ~                          # Verwendung des Goutte-Treibers für Tests
